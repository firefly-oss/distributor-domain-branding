spring:
  application:
    name: distributor-domain-branding
    version: 1.0.0
    description: Distributor Domain Branding Layer Application
    team:
      name: Firefly Software Solutions Inc
      email: dev@getfirefly.io
  config:
    import: "optional:configserver:http://app-firefly-config-server.config.svc.cluster.local:8080"
  threads:
    virtual:
      enabled: true
